# **[트러블슈팅 일지] 내 손안의 도슨트 (Docent in hand)**

이 문서는 개발 및 배포 과정에서 발생한 모든 기술적 문제, 원인 분석, 해결 과정을 기록하는 공식 트러블슈팅 일지입니다.

---

## 📌 목차
1. [TS-001: Vite 환경변수(import.meta.env) 및 TypeScript 엄격 타입 빌드 에러](#ts-001-vite-환경변수importmetaenv-및-typescript-엄격-타입-빌드-에러)
2. [TS-002: 한국향토문화전자대전 CDN 이미지 로딩 및 Fallback 스켈레톤 처리](#ts-002-한국향토문화전자대전-cdn-이미지-로딩-및-fallback-스켈레톤-처리)
3. [TS-003: 향토문화전자대전 Image API Hotlink Protection(로고 이미지 반환) 분석 및 고화질 실사 랜드마크 사진 매핑](#ts-003-향토문화전자대전-image-api-hotlink-protection로고-이미지-반환-분석-및-고화질-실사-랜드마크-사진-매핑)
4. [TS-004: 데이터 아카이브 내 비정상 이미지 링크 전수 소거](#ts-004-데이터-아카이브data-내-비정상-이미지-링크-전수-소거-및-클린-데이터셋-구축)
5. [TS-005: 모바일·폴드 명소 탐색 화면의 잘림과 하단 내비게이션 가림](#ts-005-모바일폴드-명소-탐색-화면의-잘림과-하단-내비게이션-가림)
6. [TS-006: 고정 반경 지도와 실제 지도 화면의 명소 불일치](#ts-006-고정-반경-지도와-실제-지도-화면의-명소-불일치)
7. [TS-007: 지도 마커 선택과 하단 명소 목록의 상태 불일치](#ts-007-지도-마커-선택과-하단-명소-목록의-상태-불일치)
8. [TS-008: 행정구역 클러스터의 부정확한 지역명과 과도한 정보 은닉](#ts-008-행정구역-클러스터의-부정확한-지역명과-과도한-정보-은닉)
9. [TS-009: 카카오 지도 키 입력 UI와 SDK 초기화 흐름의 복잡성](#ts-009-카카오-지도-키-입력-ui와-sdk-초기화-흐름의-복잡성)
10. [TS-010: GPS 최초 진입 시 관광지가 아닌 가장 가까운 레코드가 선택됨](#ts-010-gps-최초-진입-시-관광지가-아닌-가장-가까운-레코드가-선택됨)
11. [TS-011: 다수의 지도 마커 동시 생성으로 인한 초기 렌더링 지연](#ts-011-다수의-지도-마커-동시-생성으로-인한-초기-렌더링-지연)
12. [TS-012: .gcloudignore 패턴이 소스까지 제외해 Cloud Build 실패](#ts-012-gcloudignore-패턴이-소스까지-제외해-cloud-build-실패)
13. [TS-013: 서비스 종료된 Gemini 모델로 2계층 페르소나 생성 404](#ts-013-서비스-종료된-gemini-모델로-2계층-페르소나-생성-404)
14. [TS-014: Cloud Run 정적 응답 무압축 전송](#ts-014-cloud-run-정적-응답-무압축-전송)
15. [TS-015: Git 제외 생성 데이터 import 회귀 — 로컬은 통과, CI만 실패](#ts-015-git-제외-생성-데이터-import-회귀--로컬은-통과-ci만-실패)
16. [TS-016: Cloud Build 트리거 오설정 — 자동 감지와 서비스 계정 권한](#ts-016-cloud-build-트리거-오설정--자동-감지와-서비스-계정-권한)
17. [TS-017: Cloud Run 파일 저장소로는 방명록 공유·영속이 불가능](#ts-017-cloud-run-파일-저장소로는-방명록-공유영속이-불가능)
18. [TS-018: 동일 좌표 마커 중첩으로 검은 번짐 발생](#ts-018-동일-좌표-마커-중첩으로-검은-번짐-발생)
19. [TS-019: 카카오맵 키가 CI 빌드에 전달되지 않아 프로덕션 지도 미동작](#ts-019-카카오맵-키가-ci-빌드에-전달되지-않아-프로덕션-지도-미동작)

---

## 🛠️ 트러블슈팅 기록

### TS-001: Vite 환경변수(import.meta.env) 및 TypeScript 엄격 타입 빌드 에러
- **발생 일시:** 2026-08-20 (PoC 초기 빌드 단계)
- **발생 위치:** `src/services/geminiService.ts`, `src/App.tsx`, `src/components/*.tsx`
- **현상 및 에러 메시지:**
  ```text
  error TS2339: Property 'env' does not exist on type 'ImportMeta'.
  error TS6133: 'ExternalLink' is declared but its value is never read.
  error TS6192: All imports in import declaration are unused.
  ```
- **원인 분석:**
  1. Vite 프로젝트에서 TypeScript 컴파일러가 `import.meta.env`의 메타 타입을 인지하지 못함 (`vite-env.d.ts` 부재).
  2. `tsconfig.json`의 `noUnusedLocals`, `noUnusedParameters` 엄격 옵션으로 인해 임포트 후 사용되지 않은 심볼들이 컴파일 에러를 유발함.
- **해결 조치:**
  1. `src/vite-env.d.ts`를 생성하고 `/// <reference types="vite/client" />` 및 `ImportMetaEnv` 인터페이스 선언.
  2. 미사용 import 및 props 심볼 일괄 정리.
- **결과:** `npm run build` 성공 (0 errors).

---

### TS-002: 한국향토문화전자대전 CDN 이미지 로딩 및 Fallback 스켈레톤 처리
- **발생 일시:** 2026-08-20
- **발생 위치:** `src/components/PhotoCard.tsx`
- **현상:** 외부 공인 아카이브 이미지(`seogwipo.grandculture.net`, `jeju.grandculture.net`) 로딩 전 화면 깜빡임 또는 네트워크 일시 지연 시 깨진 이미지 아이콘 노출 가능성.
- **원인 분석:** 모바일 환경에서 고화질 CDN 이미지 다운로드 지연 시 사용자가 빈 화면을 보게 됨.
- **해결 조치:**
  1. CSS Shimmer 기반의 `image-skeleton` 애니메이션 레이어 삽입.
  2. `onLoad`, `onError` 핸들러를 통한 부드러운 페이드인 및 고해상도 백업 이미지 Fallback 로직 적용.
- **결과:** 모바일 네트워크 상태에 구애받지 않고 매끄러운 비주얼 카드 렌더링 확인.

---

### TS-003: 향토문화전자대전 Image API Hotlink Protection(로고 이미지 반환) 분석 및 고화질 실사 랜드마크 사진 매핑
- **발생 일시:** 2026-08-20
- **발생 위치:** `src/data/poiData.ts` (성산일출봉 및 전 POI 이미지)
- **현상:** 브라우저에서 `https://seogwipo.grandculture.net/Image?localName=seogwipo&id=GC046P02165&t=origin`를 요청했을 때, 실제 성산일출봉 현장 사진이 아닌 **한국향토문화전자대전의 기본 로고 심볼(청록색/갈색 파도 로고, 약 8KB)**이 렌더링됨.
- **원인 분석:**
  1. 한국향토문화전자대전 미디어 엔드포인트(`grandculture.net/Image?...`)가 외부 도메인의 Direct Hotlink를 방지하기 위해 Referer/세션이 없는 외부 요청 시 기관의 기본 로고 심볼 이미지(8,196 bytes)를 반환하도록 방어 정책이 적용되어 있음.
- **해결 조치:**
  1. 각 11개 주요 POI(성산일출봉, 산방산, 주상절리, 만장굴, 협재 해녀바당 등)에 대해 100% 매칭되는 **실제 고화질 풍경 실사 사진(한국관광공사 / 공공누리 공인 아카이브)**으로 전면 교체.
  2. 출처 및 캡션을 `한국관광공사 / 한국학중앙연구원 한국향토문화전자대전`으로 명확히 표기.
- **결과:** 성산일출봉의 웅장한 분화구와 푸른 바다 전경이 선명하게 표시됨 확인.

---

### TS-004: 데이터 아카이브(data/) 내 비정상 이미지 링크 전수 소거 및 클린 데이터셋 구축
- **발생 일시:** 2026-08-20
- **발생 위치:** `data/**/*.json` (총 18개 JSON 파일, 5,161개 학술 레코드)
- **현상:** 데이터셋 내 17,371개의 미디어 객체에 포함된 `original_url`, `thumbnail_url`, `thumb_url`, `middle_url`, `origin_url` 등이 모두 로고 심볼만 반환하여 불필요한 용량 차지 및 혼선 유발.
- **원인 분석:** 학술 텍스트와 설화 데이터는 영구 보존 가치가 높으나, 외부 이미지 URL은 서버 개편으로 인해 유효하지 않음.
- **해결 조치:**
  1. Python 심층 순회 스크립트를 작성하여 18개 JSON 파일 내 모든 비정상 이미지 URL 필드(17,371개)를 일괄 소거.
  2. 학술 텍스트, 설화, 역사적 사실, 카테고리 정보는 100% 무손실 보존.
  3. 프론트엔드에서는 검증된 저작권 프리 고화질 실사 및 사용자 준비 사진으로 완전 대체.
- **결과:** 클린 데이터셋 완성 및 불필요한 네트워크 오류 방지.

---

### TS-005: 모바일·폴드 명소 탐색 화면의 잘림과 하단 내비게이션 가림
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/App.css`, `src/components/POICarousel.tsx`
- **현상:**
  1. 폴드 크기 화면에서 데스크톱 레이아웃이 조기에 적용되어 카테고리와 명소 목록이 잘림.
  2. 명소 탐색 시트가 화면 일부 높이만 사용해 지도와 목록의 가용 영역이 부족함.
  3. 시트의 열기·닫기 애니메이션 중 하단 내비게이션이 뒤에 가려지거나 마지막 프레임에서 갑자기 사라짐.
- **원인 분석:**
  1. 모바일·데스크톱 전환 기준이 실제 폴드 화면 폭보다 낮았고, 시트에 고정 최대 높이와 상단 둥근 여백이 적용되어 있었음.
  2. 닫기 상태가 애니메이션 완료 전에 언마운트되거나 내비게이션과 시트의 쌓임 순서가 맞지 않았음.
- **해결 조치:**
  1. 폴드 크기까지 모바일 UI가 유지되도록 반응형 기준을 조정.
  2. 명소 탐색 시트를 `height: 100%`, `max-height: 100%`로 확장하고 상단 여백과 둥근 모서리를 제거.
  3. `env(safe-area-inset-top/bottom)`을 반영해 노치 및 홈 인디케이터 영역을 보호.
  4. 닫기 상태를 별도로 유지하고 500ms 애니메이션 종료 후 언마운트하도록 수정.
  5. 하단 내비게이션이 탐색 시트의 전환 중에도 앞에 남도록 레이어 순서를 정리.
- **결과:** 모바일과 폴드 화면에서 지도·카테고리·명소 목록이 잘리지 않고, 내비게이션을 유지한 채 시트가 자연스럽게 열리고 닫힘.

---

### TS-006: 고정 반경 지도와 실제 지도 화면의 명소 불일치
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/components/KakaoPOIMap.tsx`, `src/components/POICarousel.tsx`
- **현상:**
  1. 기존 1km·2km·3km 반경 필터 때문에 사용자가 지도를 확대·축소해도 마커 수가 실제 화면 범위와 맞지 않음.
  2. 지도를 이동한 뒤에도 이전 반경 기준의 명소가 남아, 지도에 보이는 지역과 표시된 마커가 어긋남.
  3. 지도 화면에 들어온 명소만 아래 목록에 남기는 방식은 사용자가 주변 전체 결과를 탐색하기 어렵게 만듦.
- **원인 분석:** GPS 중심의 고정 거리 필터와 사용자가 직접 이동하는 지도의 현재 경계(bounds)를 동일한 탐색 범위로 취급했음.
- **해결 조치:**
  1. 반경 선택 UI와 반경 원을 제거.
  2. 카카오 지도의 `idle` 이벤트에서 남서·북동 좌표를 읽어 현재 화면 경계를 React 상태로 동기화.
  3. 지도 마커는 현재 경계 안의 명소만 렌더링하고, 지도 이동이나 확대·축소가 끝날 때 다시 계산.
  4. 하단 목록은 지도 경계로 자르지 않고 전체 검색·카테고리 결과를 유지해 탐색 연속성을 보존.
  5. 초기 지도는 GPS 위치를 중심으로 레벨 6에서 시작해 약 3~4km 생활권이 보이도록 조정.
- **결과:** 지도 마커는 실제 화면 범위와 일치하고, 목록에서는 현재 화면 밖의 주변 명소도 계속 탐색할 수 있음.

---

### TS-007: 지도 마커 선택과 하단 명소 목록의 상태 불일치
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/components/KakaoPOIMap.tsx`, `src/components/POICarousel.tsx`
- **현상:** 지도 마커를 선택하면 지도 위 팝업만 표시되거나, 해당 명소가 하단 목록의 첫 화면에 없어 사용자가 선택 결과를 찾기 어려움.
- **원인 분석:** 지도 미리보기 상태와 목록 선택 상태가 분리되어 있었고, 목록은 40개 단위 지연 렌더링을 사용해 선택한 항목이 아직 DOM에 없을 수 있었음.
- **해결 조치:**
  1. 지도 위 별도 상세 팝업을 제거하고 마커 클릭을 공통 `highlightedPOIId` 상태로 연결.
  2. 선택한 명소의 목록 인덱스를 계산해 필요한 페이지까지 `visibleCount`를 즉시 확장.
  3. `useLayoutEffect`에서 해당 카드가 목록 맨 위에 오도록 즉시 스크롤하고 부드러운 스크롤은 사용하지 않음.
  4. 선택 카드와 마커에 별도 강조 상태를 적용하되, 실제 도슨트 전환은 목록 카드를 눌렀을 때 수행.
- **결과:** 마커를 누르면 지도 팝업 없이 해당 명소 카드가 목록 최상단에 바로 나타나며 선택 상태도 일치함.

---

### TS-008: 행정구역 클러스터의 부정확한 지역명과 과도한 정보 은닉
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/components/KakaoPOIMap.tsx`, `src/App.css`, POI 인덱스의 `region` 필드
- **현상:** 넓은 화면에서 핀이 과밀해 동·읍·면·리 또는 시 단위 원형 클러스터를 도입했으나, 일부 가까운 화면에서도 `서귀포시 1개`처럼 지나치게 큰 단위가 표시됨.
- **원인 분석:**
  1. POI 주소 중 다수가 `제주시` 또는 `서귀포시`까지만 제공되어 행정동을 안정적으로 추출할 수 없었음.
  2. 불완전한 주소를 기준으로 묶으면 서로 멀리 떨어진 명소가 하나의 시 단위 클러스터로 합쳐짐.
  3. 줌 레벨 임계값을 조정해도 원본 주소 정밀도 문제는 해결되지 않았고, 명소 위치 자체를 보고 싶은 서비스 목적과도 맞지 않았음.
- **해결 조치:**
  1. 행정구역·격자 클러스터 계산과 원형 개수 UI를 최종 제거.
  2. 모든 줌 단계에서 실제 좌표의 개별 명소 마커를 표시.
  3. 카카오 기본 파란 핀 대신 기존 클러스터 색상인 짙은 청록색의 텍스트 없는 커스텀 핀을 적용.
  4. 마커 호버 확대 효과를 제거하고, 선택 시에만 밝은 청록색 링으로 구분.
  5. 현재 위치는 명소 마커와 혼동하지 않도록 흰 테두리가 있는 빨간 원으로 유지.
- **결과:** 주소 정밀도와 무관하게 모든 명소의 실제 위치가 표시되며, 서비스 UI 색상과도 일관된 지도가 됨.

---

### TS-009: 카카오 지도 키 입력 UI와 SDK 초기화 흐름의 복잡성
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/components/KakaoPOIMap.tsx`, `src/services/kakaoMapService.ts`, `src/App.css`
- **현상:** 지도 위 API 키 설정 버튼·입력 폼·가이드가 일반 사용자에게 노출되어 탐색 UI를 차지하고, 키 저장 상태와 SDK 로딩 상태가 컴포넌트에 중복 관리됨.
- **원인 분석:** 개발 단계의 런타임 키 입력 기능이 운영 UI에 남아 있었고, 지도 컴포넌트가 키 설정과 지도 표현을 동시에 담당했음.
- **해결 조치:**
  1. 키 설정 버튼, 입력 폼, 저장 상태 및 관련 CSS를 제거.
  2. 지도 컴포넌트는 `kakaoMapService`에서 제공하는 환경 설정 키로만 SDK를 초기화.
  3. SDK는 `autoload=false`로 불러온 뒤 서비스에서 로딩 완료를 관리하고, 키가 없거나 로딩에 실패하면 지도 내부 대체 화면만 표시.
- **결과:** 일반 사용자는 키 입력 UI를 보지 않으며 지도 탐색 화면이 단순해짐. 배포 시에는 카카오 개발자 콘솔의 허용 도메인 설정이 별도로 필요함.

---

### TS-010: GPS 최초 진입 시 관광지가 아닌 가장 가까운 레코드가 선택됨
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/App.tsx`, `src/utils/geo.ts`, `src/components/POICarousel.tsx`, `src/components/KakaoPOIMap.tsx`
- **현상:** 앱 최초 실행에서 GPS 권한을 허용하면 전체 POI 중 직선거리가 가장 짧은 레코드가 선택되어, 설화·인물·음식 등 사용자가 기대한 관광지가 아닌 항목이 첫 도슨트로 나올 수 있었음.
- **원인 분석:** `findNearestPOI()`가 전달받은 전체 인덱스를 순회하며 거리만 비교했고 카테고리 조건을 지원하지 않았음. 3km 판정은 결과의 상태값일 뿐 후보 제한 조건이 아니었음.
- **해결 조치:**
  1. `findNearestPOI()`에 선택적 `category` 인자를 추가하고 후보 목록을 먼저 필터링하도록 변경.
  2. 최초 GPS 진입, GPS 재동기화, 좌표 직접 적용에서 모두 `관광지` 카테고리를 전달.
  3. 관광지 후보끼리는 지구 곡률을 고려한 Haversine 공식으로 직선거리를 계산해 가장 가까운 1개를 선택.
  4. GPS 실패 시에도 관광지 데이터가 존재하면 첫 관광지를 대체 항목으로 사용.
  5. 명소 탐색 목록과 지도 마커는 카테고리와 무관하게 GPS 거리순을 유지해 주변 탐색의 의미를 보존.
- **결과:** 위치 권한 허용 직후에는 가장 가까운 관광지가 첫 이야기로 선택되며, 이후 주변 탐색은 실제 거리순으로 제공됨.
- **검증:** `npx tsc --noEmit`, `npx vite build`, `git diff --check` 통과.

---

### TS-011: 다수의 지도 마커 동시 생성으로 인한 초기 렌더링 지연
- **발생 일시:** 2026-08-24
- **발생 위치:** `src/components/KakaoPOIMap.tsx`
- **현상:** 명소 탐색을 열거나 지도를 넓게 축소했을 때 현재 화면 안의 모든 커스텀 마커를 한 번에 생성해 UI가 순간적으로 멈춤.
- **원인 분석:** 각 명소마다 버튼 DOM과 카카오 `CustomOverlay`를 만드는 작업을 하나의 동기식 `forEach`에서 모두 실행해 브라우저의 메인 스레드와 첫 화면 페인트를 장시간 점유함.
- **해결 조치:**
  1. 현재 지도 경계 안의 명소를 사용자 GPS 직선거리순으로 정렬.
  2. 가장 가까운 마커부터 40개를 먼저 생성.
  3. `requestAnimationFrame`으로 브라우저 렌더링에 제어권을 돌려준 뒤 다음 40개를 누적 생성.
  4. 지도 이동·검색·카테고리 변경 또는 컴포넌트 종료 시 예약된 프레임을 취소하고 기존 오버레이를 정리해 중복 마커와 메모리 누수를 방지.
- **결과:** 가까운 명소 40개가 우선 표시되고 나머지 마커가 프레임 단위로 순차 로드되어 탐색 화면의 최초 응답성이 개선됨.

### TS-012: .gcloudignore 패턴이 소스까지 제외해 Cloud Build 실패
- **발생 일시:** 2026-08-22 (최초 Cloud Run 배포)
- **발생 위치:** `.gcloudignore`, `Dockerfile`
- **현상 및 에러 메시지:**
  ```text
  Step 8/27 : RUN npx tsc && npx vite build
  src/App.tsx(5,28): error TS2307: Cannot find module './data/characters'
  src/services/ragService.ts(2,57): error TS2307: Cannot find module '../data/jejuDialectData'
  The command '/bin/sh -c npx tsc && npx vite build' returned a non-zero code: 2
  ```
- **원인 분석:**
  1. 원본 데이터 디렉터리를 제외하려고 `.gcloudignore`에 `data`를 한 줄로 적었음.
  2. `.gcloudignore`는 gitignore 문법을 따르므로, 앵커 없는 `data`는 **모든 경로의 `data` 디렉터리**에 매칭됨. 의도한 루트 `/data/`뿐 아니라 `src/data/`까지 업로드에서 빠졌음.
  3. `src/data/characters.ts`, `jejuDialectData.ts`는 Git 에 정상 포함된 소스인데도 Cloud Build 로 전송되지 않아 모듈 해석에 실패함.
- **해결 조치:**
  1. 패턴을 `/data/`로 변경해 루트로 앵커링. 같은 이유로 `dist`, `build`도 `/dist/`, `/build/`로 수정.
  2. 제외 대상인 생성 파일은 경로를 전부 명시 (`src/data/poiData.ts` 등).
- **결과:** 재배포 성공. 리비전 `docent-in-hand-00001-l28` 생성.
- **교훈:** `.gcloudignore`가 없으면 gcloud 는 `.gitignore`를 대신 읽는다. 두 파일 모두 gitignore 문법이므로 디렉터리 제외는 반드시 앞에 `/`를 붙여 앵커링할 것.

---

### TS-013: 서비스 종료된 Gemini 모델로 2계층 페르소나 생성 404
- **발생 일시:** 2026-08-22 (배포 직후 SSE 검증)
- **발생 위치:** `server/src/config/env.ts`, `.env.example`, `src/services/geminiService.ts`
- **현상:** 1계층 리서치 에이전트는 `research_complete`까지 정상 진행되나, 2계층 페르소나 스토리 생성 단계에서 SSE `error` 이벤트로 종료됨.
  ```text
  event: error
  data: {"code":404,"message":"This model models/gemini-2.0-flash is no longer available.
         Please update your code to use models/gemini-3.6-flash"}
  ```
- **원인 분석:**
  1. Cloud Run 환경변수 `GEMINI_MODEL`을 `.env.example`의 기본값 `gemini-2.0-flash`로 설정했음.
  2. 해당 모델은 이미 서비스가 종료되어 API 가 404 를 반환함.
  3. 로컬 `.env`는 `gemini-3.7-flash`로 갱신돼 있어 개발 환경에서는 재현되지 않았음.
- **해결 조치:**
  1. Cloud Run 환경변수를 `gemini-3.7-flash`로 교체.
  2. 코드 전반의 기본값(`env.ts`, `.env.example`, `geminiService.ts`)도 함께 갱신해 같은 실수가 재발하지 않도록 함.
- **결과:** `agent_status ×6 → research_complete → persona_stream ×6 → done` 정상 완료. 응답 3.85초, 근거 출처 5건 포함.
- **교훈:** `.env.example`은 문서가 아니라 실제 기본값으로 쓰이는 경우가 있으므로, 모델명처럼 수명이 있는 값은 함께 갱신해야 한다.

---

### TS-014: Cloud Run 정적 응답 무압축 전송
- **발생 일시:** 2026-08-22
- **발생 위치:** `server/src/index.ts`
- **현상:** 프론트 번들이 gzip 없이 606KB 그대로 전송됨. 응답에 `Content-Encoding` 헤더 자체가 없음.
  ```text
  $ curl -sI -H 'Accept-Encoding: gzip' .../assets/index-*.js
  content-length: 606589        # Content-Encoding 없음
  ```
- **원인 분석:**
  1. 기존 nginx 구성에는 `gzip on`이 있었으나, 배포 구조를 Express 단일 서비스로 바꾸면서 그 설정이 사라짐.
  2. `express.static`은 압축 기능을 제공하지 않는다. 별도 미들웨어가 없으면 원본 그대로 전송된다.
- **해결 조치:**
  1. `compression` 미들웨어 추가.
  2. SSE 회귀를 막기 위해 filter 로 두 경우를 제외 — `text/event-stream`(스트리밍이 버퍼링되어 끊겨 보임), 이미 `Content-Encoding`이 설정된 응답(GCS 에서 gzip 그대로 통과시키는 `/data` 프록시).
- **결과:** 번들 전송량 606KB → 137KB. SSE 이벤트 흐름 회귀 없음 확인.

---

### TS-015: Git 제외 생성 데이터 import 회귀 — 로컬은 통과, CI만 실패
- **발생 일시:** 2026-08-23
- **발생 위치:** `src/App.tsx`, `src/components/POICarousel.tsx`, `src/utils/themeTitle.ts`
- **현상:** main 에 머지된 뒤 Cloud Build 가 실패하고 자동 배포가 멈춤. 개발자 로컬에서는 `npm run build`가 정상 통과해 원인 파악이 지연됨.
  ```text
  src/App.tsx(4,26): error TS2307: Cannot find module './data/poiData'
  src/components/POICarousel.tsx(3,26): error TS2307: Cannot find module '../data/poiData'
  src/utils/themeTitle.ts(2,29): error TS2307: Cannot find module '../data/ragKnowledgeBase'
  ... implicit any 14건 (POI_LIST 타입 소실로 인한 연쇄) — 총 17건
  ```
- **원인 분석:**
  1. GCS 전환으로 화면 컴포넌트가 `poiDataService`(런타임 fetch)를 쓰도록 바꿔 두었는데, 이후 두 PR 에서 다시 `import { POI_LIST } from './data/poiData'` 형태로 되돌아감.
  2. `src/data/poiData.ts`, `ragKnowledgeBase.ts`, `ragFullCorpus.json`은 `.gitignore` 대상(생성 파일)이라 저장소에 없음.
  3. **로컬에는 `npm run sync:data` 산출물이 디스크에 남아 있어 빌드가 통과**하지만, Cloud Build 는 클린 체크아웃이라 파일이 없어 실패함. 로컬 검증이 CI 를 대변하지 못하는 구조였음.
  4. 직전 커밋 "resolve minor TypeScript types"는 이 오류를 잡으려는 시도였으나, 근본 원인이 누락된 모듈이라 해결되지 않았음.
- **해결 조치:**
  1. `origin/main`을 별도 워크트리로 체크아웃해 클린 상태를 재현하고 오류를 확인.
  2. 컴포넌트를 다시 `loadPOIIndex()` / `loadPOICards()` 기반으로 복원. 새로 들어온 기능(거리 정렬, 이웃 POI 네비게이션 등)은 건드리지 않고 데이터 접근 경로만 되돌림.
  3. 시트 딥 서치를 유지하기 위해 `poi-cards.json`에 `mythTitle`·`sampleQuestions` 추가(gzip 329KB → 408KB). 원문 본문(`details`)은 gzip 1.5MB 로 불어나 제외하고, 본문 검색은 백엔드 `ArchiveSearchTool`이 담당하도록 정리.
  4. 카드 스키마가 바뀌었고 GCS 객체가 `immutable` 캐시이므로 `DATA_VERSION`을 v2 로 올려 재업로드.
- **결과:** CI 빌드 성공, 리비전 `docent-in-hand-fbffee4` 배포. 초기 전송량 197.7KB 유지.
- **검증 방법:** 생성 데이터 3종을 임시로 이동시킨 뒤 `npx tsc --noEmit`을 실행해 클린 체크아웃 조건을 재현. 0 오류 확인 후 원복.
- **교훈:** Git 제외 생성 파일을 쓰는 프로젝트에서 로컬 빌드 성공은 CI 성공을 보장하지 않는다. 배포 전 검증은 반드시 클린 상태를 재현해서 할 것.

---

### TS-016: Cloud Build 트리거 오설정 — 자동 감지와 서비스 계정 권한
- **발생 일시:** 2026-08-22 (CI/CD 최초 구성)
- **발생 위치:** Cloud Build 트리거 `deploy-main`, `cloudbuild.yaml`
- **현상:** 트리거 생성 화면의 기본값을 그대로 두면 두 가지 방식으로 실패함.
- **원인 분석:**
  1. **구성 유형 `자동 감지`** — 저장소에서 `cloudbuild.yaml` 또는 `Dockerfile`을 찾는데 이 저장소에는 둘 다 있다. Dockerfile 이 선택되면 이미지 빌드·푸시만 수행하고 **배포 스텝이 실행되지 않는다.** 빌드는 성공(초록불)으로 표시되는데 Cloud Run 은 그대로여서 가장 알아채기 어려운 실패가 된다.
  2. **서비스 계정** — 조직 정책 `cloudbuild.disableCreateDefaultServiceAccount`가 적용(enforced) 상태라 사용자 관리형 계정을 지정해야 한다. 1세대 기본값인 `<프로젝트번호>@cloudbuild.gserviceaccount.com`은 `roles/cloudbuild.builds.builder`만 보유해 `run.services.update`와 `iam.serviceAccounts.actAs`가 없다. 빌드·푸시는 통과하고 **마지막 배포 스텝에서만 권한 오류**가 난다.
- **해결 조치:**
  1. 구성 유형을 `Cloud Build 구성 파일(YAML)` + 위치 `저장소` / `cloudbuild.yaml`로 명시.
  2. 서비스 계정을 `<프로젝트번호>-compute@developer.gserviceaccount.com`으로 지정. 이 계정은 `roles/editor`를 보유해 필요한 권한 5종(`artifactregistry` 업로드·다운로드, `run.services.update`, `iam.serviceAccounts.actAs`, `logging.logEntries.create`)을 모두 충족함을 사전 확인함. 추가 IAM 부여는 불필요했음.
  3. `cloudbuild.yaml`에서 이미지 태그와 리비전 접미사에 `$COMMIT_SHA`를 사용해, 배포된 리비전이 어느 커밋인지 이름만으로 추적되도록 함(`docent-in-hand-<SHA>`).
  4. `min/max-instances`는 의도적으로 선언하지 않음 — 시연 여부에 따라 조정하는 비용 설정이라 배포마다 되돌아가면 안 됨. gcloud 는 미지정 설정을 기존 값 그대로 유지한다.
- **결과:** PR 머지 → 자동 빌드 → 배포까지 무개입 동작 확인. 리비전이 머지 커밋 SHA 와 일치.
- **교훈:** 이전에는 로컬 워킹트리를 zip 으로 올리는 `gcloud run deploy --source .` 방식이라 브랜치·커밋과 무관하게 배포됐고 추적이 불가능했다. 커밋 SHA 기반 태깅으로 이 문제를 함께 해소함.

---

### TS-017: Cloud Run 파일 저장소로는 방명록 공유·영속이 불가능
- **발생 일시:** 2026-08-24
- **발생 위치:** `server/src/services/communityStoryService.ts`
- **현상:** 방명록을 `server/data/community_stories.json` 파일에 저장하는 초기 구현은 로컬에서는 정상 동작하나, Cloud Run 배포 환경에서는 사용자 간 글 공유가 되지 않고 글이 소실될 수 있음.
- **원인 분석:**
  1. **인스턴스 격리** — `max-instances=5` 설정이라 최대 5개 인스턴스가 각자 독립된 파일시스템을 가진다. 사용자 A 의 글이 인스턴스 #2 에 저장되면 #4 로 라우팅된 사용자 B 는 볼 수 없다. 파일 쓰기 락은 한 인스턴스 안에서만 유효해 이 문제를 막지 못한다.
  2. **휘발성** — 컨테이너 파일시스템은 인메모리(tmpfs)다. 인스턴스가 재활용되거나 새 배포가 일어나면 저장된 글이 전부 사라진다. 2Gi 메모리 한도도 함께 소모한다.
  3. **공감 카운터 경합** — read-modify-write 구조라 동시 요청 시 갱신이 유실된다. GCS 단일 JSON 객체로 옮겨도 동일한 문제가 발생한다.
  4. **사진 첨부 실패** — `FileReader`로 원본을 base64 화해 그대로 POST 하는 구조였는데, 3MB 사진이 base64 4MB 가 되어 `express.json` 기본 한도 100kb 를 41배 초과. 사진을 첨부하면 HTTP 413 으로 등록 자체가 실패하고 있었음.
- **해결 조치:**
  1. 저장소를 Firestore(Native, `asia-northeast3`)로 교체. 컬렉션 `communityStories`.
  2. 공감은 `FieldValue.increment`로 원자 처리해 갱신 유실 제거.
  3. 시드는 고정 문서 ID + 존재 시 건너뛰기로 변경. 매 기동 적재 방식이면 재시작마다 5건씩 늘고 사용자 공감 수도 초기화된다.
  4. 사진은 클라이언트에서 캔버스로 긴 변 1280px JPEG 로 축소(보통 300KB) → `POST /api/stories/photo`로 비공개 GCS 버킷에 저장 → 문서에는 `/media/...` 경로만 기록. `/media` 프록시가 서비스 계정으로 중계하므로 버킷은 계속 비공개.
  5. 인증 없는 공개 엔드포인트이므로 쓰기 요청에 IP 당 분당 20회 레이트 리밋 적용.
- **결과 및 검증(프로덕션 실측):**
  - 다중 인스턴스 공유: 글 작성 후 20회 분산 조회 → 20/20 동일하게 조회됨.
  - 공감 동시성: 병렬 +1 요청 10회 → likes 정확히 10 (갱신 손실 없음).
  - 레이트 리밋: 연속 30회 → 8 통과 / 22 차단(429).
  - 사진 업로드 → `/media` 재조회 200 `image/png`.
- **교훈:** Cloud Run 처럼 상태 없는 실행 환경에서 파일시스템은 캐시로만 취급해야 한다. 사용자 데이터는 외부 관리형 저장소로 분리할 것.

---

### TS-018: 동일 좌표 마커 중첩으로 검은 번짐 발생
- **발생 일시:** 2026-08-24
- **발생 위치:** `build/gcs/assets/v1/poi-index.json`, `src/components/KakaoPOIMap.tsx`, `src/App.css`
- **현상:** 제주 시청 인근에서 마커 주변이 검은 스프레이처럼 번져 보임.
- **원인 분석:**
  1. 원본 자료에 정확한 위치가 없는 다수의 레코드가 공통 대체 좌표 `33.4996, 126.5312`를 사용함.
  2. 해당 좌표에 1,865개 POI가 존재하며, 순차 로딩된 첫 40개 마커도 완전히 같은 위치에 겹침.
  3. 커스텀 핀마다 적용된 반투명 그림자가 누적되어 검은 번짐처럼 렌더링됨.
- **해결 조치:**
  1. 현재 지도 경계 안의 POI를 `위도:경도` 키로 그룹화.
  2. 완전히 같은 좌표는 지도에 하나의 대표 마커만 생성.
  3. 그룹에 포함된 모든 POI는 하단 목록에서 그대로 유지해 데이터 접근성을 보존.
  4. 그룹 내 어느 POI가 선택되어도 대표 마커가 선택 상태로 표시되도록 연결.
- **결과:** 동일 좌표의 그림자 중첩과 불필요한 DOM 생성을 제거하면서 전체 명소 목록은 손실 없이 유지됨.

---

### TS-019: 카카오맵 키가 CI 빌드에 전달되지 않아 프로덕션 지도 미동작
- **발생 일시:** 2026-08-24 (지도 기능 main 머지 직후)
- **발생 위치:** `Dockerfile`, `cloudbuild.yaml`, `src/services/kakaoMapService.ts`
- **현상:** 지도 기능이 머지되어 CI 빌드가 성공하고 배포도 완료되었으나, 프로덕션 번들에 카카오맵 키가 비어 있어 SDK 를 초기화할 수 없는 상태였음. 빌드·배포가 모두 초록불이라 파이프라인만 봐서는 문제를 알 수 없음.
  ```bash
  # 배포된 번들에서 키 검색 → 결과 없음
  $ curl -s .../assets/index-*.js | grep -c "<카카오 키>"
  0
  ```
- **원인 분석:**
  1. `kakaoMapService.ts`는 `import.meta.env.VITE_KAKAO_MAP_API_KEY`를 읽는다. Vite 는 이 값을 **빌드 시점에 번들 문자열로 인라인**한다.
  2. 따라서 Cloud Run 의 런타임 환경변수(`--set-env-vars`)로는 **절대 반영되지 않는다.** 값이 필요한 시점은 컨테이너 실행 시가 아니라 `docker build` 안의 `vite build` 시점이다.
  3. 기존 파이프라인에는 빌드 단계로 값을 전달하는 경로 자체가 없었고, `.env`는 `.dockerignore`로 제외되어 컨테이너 안에서 참조할 수도 없었다.
  4. **로컬에서는 재현되지 않는다.** Vite 가 프로젝트 루트의 `.env`를 자동으로 읽어 개발자 환경에서는 항상 키가 채워지기 때문이다.
- **해결 조치:**
  1. `Dockerfile`에 `ARG VITE_KAKAO_MAP_API_KEY` / `ENV`를 `vite build` 이전 단계에 선언.
  2. `cloudbuild.yaml`의 build 스텝을 `entrypoint: bash`로 바꾸고 `availableSecrets`로 Secret Manager(`kakao-map-key`)를 읽어 `--build-arg`로 전달.
  3. Cloud Build 는 `$VAR`를 자체 치환 변수로 해석하므로, 셸 변수로 넘기려면 **`$$KAKAO_MAP_KEY`로 이스케이프**해야 한다. 이 처리를 빠뜨리면 빈 문자열이 주입되어 증상이 그대로 재현된다.
  4. Cloud Build 서비스 계정에 `kakao-map-key` `secretAccessor` 권한 부여.
  5. 카카오 개발자 콘솔에 배포 도메인 2개(`*.run.app`)를 허용 도메인으로 등록.
- **결과 및 검증:**
  - 프로덕션 번들에 키 인라인 확인, 리비전 `docent-in-hand-6ad01d5` 배포.
  - 등록한 배포 도메인 2개를 Referer 로 SDK 요청 → 둘 다 HTTP 200 정상 반환.
  - 미등록 도메인 대조군 → `AccessDeniedError: domain mismatched!` 로 차단 확인.
- **검증 방법:** `.env`를 임시로 이동시켜 Docker 빌드 조건(`.dockerignore`로 `.env` 제외)을 재현. 빌드 인자 없이 빌드하면 번들에 키가 없고, `VITE_KAKAO_MAP_API_KEY`를 주면 인라인되는 것을 각각 확인한 뒤 원복.
- **교훈:**
  1. `VITE_` 접두사 환경변수는 **빌드 타임 변수**다. 런타임 환경변수와 주입 경로가 완전히 다르므로 배포 파이프라인에서 구분해 다뤄야 한다.
  2. 브라우저 SDK 키는 번들 노출이 불가피하다. 비밀 유지가 아니라 **허용 도메인 설정**으로 보호하는 것이 정석이며, 저장소에 값을 남기지 않기 위해서만 Secret Manager 를 경유한다.
  3. TS-015 와 같은 계열의 함정이다 — 로컬 빌드 성공이 CI·프로덕션 정상을 보장하지 않는다.
