# 🏛️ [해커톤] 기존 구조 아키텍처 명세서 (Legacy Architecture)

본 문서는 **Docent-in-Hand (내 손안의 제주 도슨트)**의 초기 프로토타입/PoC 버전에서 사용된 **프론트엔드 단독(In-Memory) RAG 기반 아키텍처**를 상세히 기록하고 분석합니다.

---

## 1. 📊 기존 아키텍처 개요 및 다이어그램

기존 구조는 별도의 백엔드 서버 없이 브라우저(Client) 내부에서 모든 로직(GPS 연산, 3,285건 지식베이스 인덱싱, Gemini API 직접 호출)을 처리하는 **클라이언트 완결형 싱글 LLM 구조**였습니다.

```mermaid
graph TD
    subgraph "📱 Frontend Client (React + Vite SPA)"
        GPS[📍 브라우저 Geolocation API<br>useGPS / 하버사인 공식]
        SimModal[🎛️ GPS 시뮬레이터 모달]
        
        subgraph "💾 클라이언트 메모리 적재 (In-Memory)"
            POIList[📍 30대 명소 메타<br>poiData.ts]
            CorpusJSON[📚 18종 3,285건 데이터셋<br>ragFullCorpus.json - 9.8MB]
        end

        subgraph "⚡ 클라이언트 RAG 엔진"
            RAGEngine[FullRagSearchEngine.ts<br>키워드/카테고리 유사도 검색]
            RAGPrompt[RAGService.ts<br>단일 프롬프트 빌더]
        end

        subgraph "🤖 AI 연동 계층"
            GeminiSDK[@google/genai SDK<br>GeminiService.ts]
            API_KEY[🔑 브라우저 로컬 스토리지 / .env]
        end

        subgraph "🖥️ 화면 출력 및 음성"
            UI[📱 모바일 반응형 웹 UI]
            StoryView[📜 3막 도슨트 텍스트 뷰어]
            TTS[🔊 브라우저 Web Speech API]
        end
    end

    subgraph "☁️ 외부 Google AI Cloud"
        GeminiFlash[🤖 Gemini 2.0 Flash Endpoint]
    end

    %% 데이터 흐름
    GPS & SimModal -->|위경도 계산| POIList
    POIList -->|장소 ID & 키워드| RAGEngine
    CorpusJSON --> RAGEngine
    RAGEngine -->|인출된 텍스트 청크| RAGPrompt
    RAGPrompt -->|단일 융합 프롬프트| GeminiSDK
    API_KEY --> GeminiSDK
    GeminiSDK -->|HTTPS REST / WebSocket| GeminiFlash
    GeminiFlash -->|단일 스트림 반환| GeminiSDK
    GeminiSDK --> StoryView --> TTS
    StoryView --> UI
```

---

## 2. 🧩 계층별 구성 요소 및 동작 방식

| 계층 (Layer) | 주요 파일 | 세부 역할 및 구현 방식 |
|:---|:---|:---|
| **1. 위치 및 감각 계층** | `src/hooks/useGPS.ts`<br>`src/components/GPSSimulatorModal.tsx` | • 모바일 GPS 좌표를 1초 주기로 폴링.<br>• Haversine 공식을 사용해 30개 명소 중 반경 250m 이내 진입 시 자동 감지(Zero-Click).<br>• 미현장 개발/테스트용 GPS 시뮬레이터 제공. |
| **2. 데이터 및 지식 계층** | `src/data/poiData.ts`<br>`src/data/ragFullCorpus.json` | • 30개 핵심 명소 메타데이터(이름, 좌표, 이미지, 캐릭터).<br>• 한국학중앙연구원 18종 3,285건 학술 문서(9.8MB)를 프론트엔드 번들에 직접 포함. |
| **3. RAG 검색 계층** | `src/services/fullRagSearchEngine.ts`<br>`src/services/ragService.ts` | • 브라우저 메모리에 로드된 3,285건을 자바스크립트 루프로 순회 검색.<br>• 설화, 역사, 지질, 민속 4대 범주에서 상위 N개 문서를 뽑아 하나의 거대 프롬프트로 병합. |
| **4. AI 추론 계층** | `src/services/geminiService.ts` | • `@google/genai` SDK를 브라우저에서 직접 인스턴스화.<br>• 프롬프트와 1인칭 캐릭터 시스템 프롬프트를 묶어 1회성 `generateContentStream` 호출. |
| **5. 프레젠테이션 계층** | `src/components/StoryCard.tsx`<br>`src/components/ChatSection.tsx`<br>`src/hooks/useAudioPlayer.ts` | • 실시간 토큰 텍스트 스트리밍 렌더링.<br>• Web Speech API를 통한 기본 브라우저 TTS 음성 낭독. |

---

## 3. ⚖️ 기존 구조의 장점과 한계점 분석

### ✅ 장점
1. **빠른 초기 PoC 개발**: 백엔드 서버 없이 프론트엔드 코드만으로 동작을 검증할 수 있음.
2. **초저지연 RAG 검색**: 네트워크 경유 없이 브라우저 메모리에서 1ms 만에 문서 검색 완료.
3. **인프라 비용 없음**: 서버 유지비가 들지 않고 정적 호스팅(GitHub Pages / Vercel) 가능.

---

### ⚠️ 치명적 한계점 및 개선 필요 사항
1. **보안 취약점 (API Key 노출 위험)**:
   - Google Gemini API Key가 클라이언트 브라우저의 소스코드 및 Network 요청 탭에 직접 노출됨.
2. **클라이언트 번들 비대화 (메모리 부담)**:
   - 3,285건의 JSON 데이터(약 9.8MB)를 브라우저가 다운로드하고 메모리에 상주시켜야 하므로 모바일 기기에서 초기 로딩 속도 저하 및 메모리 부담 발생.
3. **단일 LLM의 한계 (Hallucination 및 역할 충돌)**:
   - 하나의 모델이 **"방대한 데이터 속 팩트 검증"**과 **"1인칭 페르소나 연기"**를 동시에 수행하다 보니, 가끔 역사적 연도나 설화 내용이 왜곡되거나 캐릭터 말투가 무너지는 현상 발생.
4. **도구 활용 및 다단계 추론 불가 (Passive RAG)**:
   - 정해진 1회성 RAG 검색 외에, 사용자의 복합 질문("성산일출봉 보고 근처 해녀 맛집이랑 야경 코스 짜줘")에 맞춰 스스로 추가 정보를 탐색하고 계획을 세우는 에이전틱(Agentic) 추론이 불가능함.
